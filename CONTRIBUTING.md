# Contributing to TrustFlow Dashboard

We welcome UI/UX improvements and feature additions!

1. **Branch Naming:** Use `feat/`, `fix/`, or `ui/`.
2. **State Management:** For global state (like wallet connection), use Zustand. Do not clutter components with prop drilling.
3. **Pull Requests:** Any PR that modifies the UI must include a before/after screenshot.